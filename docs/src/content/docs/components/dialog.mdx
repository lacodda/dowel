---
title: Dialog
description: Say in one line what this is for.
---

import Stand from '../../../components/Stand.astro'

```console
$ npx shadcn@latest add https://lacodda.github.io/dowel/r/dialog.json
```

<Stand component="dialog" caption="Say what the reader will see there." />

## Notes

Say why it is the way it is - the page explains, the stand shows.

## Props

| Prop | Type | Default | |
| --- | --- | --- | --- |
| `variant` | `default` | `default` | |
| `className` | `string` | | Merged so the caller wins a conflict |
