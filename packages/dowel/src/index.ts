// Public surface of the `dowel` package. The theme (tokens as CSS custom
// properties plus a Tailwind `@theme` block) lands with v0.1.0; primitives are
// distributed through the registry, not through this entry point.
export const version = '0.0.0'
